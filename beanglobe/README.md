# BeanGlobe

A coffee discovery, reviews & matching platform. (Evolution of the original PHP webshop.)

> TODO (Dávid, no AI): write this yourself once the spine is built — it's your ownership signal.

## Structure
- `apps/backend` — Node/Express API (the current focus)
- `apps/web` — React frontend (later)
- `apps/mobile` — React Native / Expo (later still)
- `db` — schema, migrations, seed data
