// SERVICE (business logic, no HTTP/SQL): Spec/rule-based recommender for machines (budget, type, features). Deliberately SEPARATE from matchingService — different mechanism, must not share code.
//
// TODO: build Socratically.
