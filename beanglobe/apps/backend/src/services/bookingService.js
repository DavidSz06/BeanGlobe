// SERVICE (business logic, no HTTP/SQL): Booking business logic: timeslot generation, collision handling, and the state machine (pending -> confirmed/rejected). Triggers email (via EmailSender) and iCal export (via lib/ical.js).
//
// TODO: build Socratically.
