// SERVICE (business logic, no HTTP/SQL): Business logic for reviews (coffee reviews feed the taste vector; machine reviews are generic). Handles the flagged/moderation state.
//
// TODO: build Socratically.
