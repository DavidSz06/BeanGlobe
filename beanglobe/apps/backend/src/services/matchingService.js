// SERVICE (business logic, no HTTP/SQL): THE OWNABLE CORE. Ranks coffees for a user by cosine similarity between the user's taste vector and each coffee's flavor_vector. Pure domain logic — knows nothing about HTTP or SQL. Math lives in lib/vectorMath.js. Build this BY HAND.
//
// TODO: build Socratically.
