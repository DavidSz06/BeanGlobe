// SERVICE (business logic, no HTTP/SQL): Business logic for products: assemble a coherent product (base + details) via productRepository, apply filters, delegate coffee ranking to matchingService and machine picks to machineRecommenderService.
//
// TODO: build Socratically.
