// ADAPTER INTERFACE: sending booking notification emails.
//   send({ to, subject, body, icalAttachment })
// Concrete impl (SMTP / provider) added later — bookingService depends on this shape.
//
// TODO: build Socratically.
